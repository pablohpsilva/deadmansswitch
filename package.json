{
  "name": "deadmansswitch",
  "private": true,
  "version": "1.0.0",
  "description": "A secure, decentralized Dead Man's Switch application",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "turbo format",
    "clean": "turbo clean",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "turbo": "^1.10.0",
    "@biomejs/biome": "^1.4.0"
  },
  "packageManager": "pnpm@8.10.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
